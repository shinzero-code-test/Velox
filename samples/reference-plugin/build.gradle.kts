plugins {
    id("com.android.application") version "8.13.0" apply false // standalone; use your AGP version
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}

// This file is a template — not applied when building Velox.
// Copy into a standalone project with its own settings.gradle.kts:
//
// include(":app")
// project(":app").projectDir = file(".")

